{% extends "base.twig" %}

{% block content %}
	<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<section class="article-content">
			<h1 class="article-h1">{{post.title}}</h1>
			<div class="article-body">
				{{post.content}}
			</div>
			<div class="wall">
				{% for item in wall %}
					{% set link = item.link %}
					{% for item in item.gallery %}
						<div class="">
							<a href="{{link}}">
								<img class="" src="{{Image(item).src('thumbnail')}}" alt="Thumbnail for wall item {{item}}">
							</a>
						</div>
					{% endfor %}
				{% endfor %}
			</div>
		</section>
	</article>
{% endblock %}